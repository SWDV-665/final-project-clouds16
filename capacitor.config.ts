import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AdvancedTopics',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
